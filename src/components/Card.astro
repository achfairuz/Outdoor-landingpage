---
const { title, src } = Astro.props;
---

<div class='flex flex-col justify-center w-40 md:w-60 p-2'>
  <h3 class='text-md text-center mb-2 font-semibold'>{title}</h3>
  <img src={src} alt={title} class='w-full' draggable='false' />
</div>
